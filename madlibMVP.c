#include <stdio.h>
#include <stdlib.h>

/*
  Input:
  Output:
*/
char find_madlib(num) {
  // TODO MJ
  copyMad = copy(emptyMad) //copy doesn't actually work, must write it
  return copyMad;
}

/*
  Input:
  Output:
*/
char parse_madlib(copyMad) {
  // TODO Gracey
  return completedMad;
}

/*
  Input:
  Output:
*/
int main() {
  // TODO together
  fgets("Type in a number: one or two ")
  //save the input from fgets as num
  emptyMad = find_madlib(num);
  completedMad = parse_madlib(emptyMad);
  printf("%s\n", completedMad);
  return 0;
}
